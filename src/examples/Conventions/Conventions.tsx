// camelCase
// When to Use:
// Variable names: let userAge = 25;
// Function names: function calculateSum(a, b) { return a + b; }
// Event handlers: onClick, onChange, handleSubmit

// Object keys in JSON or state: { userName: "John", userAge: 25 }Why camelCase?
// Common in JavaScript coding standards.
// Improves readability by distinguishing between lowercase initial letters (variables) and uppercase initials (classes).
// PascalCase

// When to Use:
// Component names: RectangleComponent, UserProfile

// Class names (rare in modern React): class UserManager {}Why PascalCase?
// React enforces PascalCase for component names to distinguish them from HTML elements.
// Enhances clarity in JSX, e.g., <UserProfile /> instead of <userProfile />.
// snake_case
// When to Use:
// File and folder names in some projects: user_profile.js, rectangle_area.css

// Rarely used in React directly but may appear in certain backend APIs or legacy code. Why snake_case?
// Consistent with some backend systems or database naming conventions.
// Avoids confusion when working in environments where underscores are preferred.
