import { Child } from "./ChildComponent";

// Parent Component
export function Parent() {
  return (
    <div>
      <h1>Welcome to the Parent Component</h1>
      <Child />
    </div>
  );
}
